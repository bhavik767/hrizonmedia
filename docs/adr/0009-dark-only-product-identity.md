# Dark-only HrizonMedia identity

Issue #29 supersedes ADR-0003's derived light theme with a dark-only product identity using the approved palette, typography and key/play mark with the HrizonMedia wordmark. Removing the theme toggle keeps the landing page, Demo and shared navigation consistent; brand and accessibility checks cover the resulting single visual system.
